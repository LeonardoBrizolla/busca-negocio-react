import IconClose from '../../components/IconClose';

export default {
  title: 'components/IconClose',
  component: IconClose,
  parameters: {
    layout: 'centered',
  },
};

export function Default() {
  return <IconClose />;
}
