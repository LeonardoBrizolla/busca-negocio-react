import IconArrow from '../../components/IconArrow';

export default {
  title: 'components/IconArrow',
  component: IconArrow,
  parameters: {
    layout: 'centered',
  },
};

export function Default() {
  return <IconArrow />;
}
