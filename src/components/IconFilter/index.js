import styled from 'styled-components';
import icon from '../../assets/images/icon-filter.svg';

const IconFilter = styled.img.attrs({ src: icon, alt: 'Ícone de filtro' })``;

export default IconFilter;
